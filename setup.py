from setuptools import setup, find_packages

setup(
    name='TinyTorch',
    version='0.1',
    description='A bare bones deep learning framework with PyTorch like syntax.',
    author='hardyek',
    packages=find_packages(),
)